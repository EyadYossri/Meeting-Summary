[build]
builder = "nixpacks"

[phases.setup]
aptPkgs = [
  "ffmpeg",
  "libavcodec-extra",
  "libavformat-dev",
  "libavutil-dev",
  "libswresample-dev"
]

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
